import { SortByDayPipe } from './sort-by-day.pipe';

describe('SortByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
